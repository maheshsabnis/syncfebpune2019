var mymath = function() {};

mymath.prototype.add = function(a, b) {
  return a + b;
};

mymath.prototype.sub = function(a, b) {
  return a - b;
};

mymath.prototype.mult = function(a, b) {
  return a * b;
};
